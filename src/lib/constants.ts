import { PUBLIC_DEV_TO_USERNAME } from '$env/static/public';

export const baseAPI = 'https://dev.to/api';
export const articleAPI = `${baseAPI}/articles`;
export const DevToUsername = PUBLIC_DEV_TO_USERNAME;
